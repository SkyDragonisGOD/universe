# 世界生成器 - 开发日志

## v1.9.4 (2026-08-18)

### 项目概览 — 展示/编辑模式 + 标签系统

- **展示/编辑模式**：默认展示页（只读显示世界名称、类型标签、标签、简介），点击「✏️ 编辑」进入编辑模式，点击「✓ 完成」回到展示页
- **类型/风格标签化**：预设 20 个类型（奇幻、科幻、蒸汽朋克、赛博朋克、末日废土、历史、现代、神话、仙侠、武侠…）+ 自定义输入 + 点击多选/取消，选中项以紫色标签显示
- **标签标签化**：预设 30 个世界观常见标签（魔法体系、多物种、架空大陆、星际旅行、平行世界、政治阴谋…）+ 自定义输入 + 点击多选/取消，选中项以绿色标签显示
- **数据兼容**：`genre` 字段支持旧字符串格式自动转数组（`split(',')`），`tags` 保持数组格式
- **新增 CSS**：`.ov-preset-list`、`.ov-tag`、`.ov-genre`、`.ov-custom-row`、`.wiki-field`、`.wiki-label`、`.wiki-value` 标签选择器样式
- **仪表盘移除领地统计**：从概览数据统计中移除「🗺️ 领地」项

### 关系图谱 — 列表重做与跨tab关系

- **关系列表重做**：左侧列表与右侧图谱使用同一数据源 `_collectAllConnections`，显示所有实体类型间的关联（角色↔角色、角色↔地点、势力↔地点、事件↔物品…），不再仅限角色关系
- **跨tab关系支持**：新增 `entityRelations` 数据结构（`{id, fromId, fromType, toId, toType, type, description}`），支持任意实体类型间建立显式关系
- **新建关系弹窗**：选择源类型+源实体、目标类型+目标实体、关系类型、描述，写入 `entityRelations`
- **关系列表项**：显示类型图标（👤🏰📍📦⚡）+ 实体名 + 关系描述 + 类型徽章（如「角色 → 地点 · 可编辑」），支持搜索过滤
- **详情面板**：选中关系显示详情，显式关系可编辑类型/描述、可删除；隐式关系（源自实体数据字段）提示「编辑对应实体可修改」
- **图谱与列表同步**：点击图谱节点高亮时，左侧列表自动过滤为该节点的所有关系，标题变为「📋 实体名 的关系 (N)」，出现「✕ 取消聚焦」按钮；取消聚焦恢复全部
- **勾选主体即刷新列表**：`toggleGraphSubjectFromFilter` 勾选/取消主体时，调用 `_syncRelationList()` 立即刷新左侧列表
- **新增 CSS**：`.relation-entity`、`.relation-type-badge` 样式

### 关系图谱 — 拖拽物理优化

- **拖拽停住不弹**：节点在画布内且距鼠标 < 1.5px 时，直接吸附到鼠标位置、速度归零，不再有弹簧余振；仅在超出边界时继续弹簧跟随（产生拉扯感）
- **修复 typo**：`BOUNDARY_OVERALLOW_OFFSET = 45;45;` → `45;`

### 关系图谱 — 拆分模块

- **relations-data.js**：数据层（实体类型、查询、连接收集、边构建、视口、位置），10 个函数
- **relations-graph.js**：图谱引擎（物理常量、弹簧阻尼模拟、画布交互、绘制原语、主绘制、入场动画、缩放/布局控制），11 个函数
- **relations-ui.js**：UI层（HTML渲染、列表/详情、筛选控制、CRUD操作、入口函数），10 个函数
- **加载顺序**：data → graph → ui，旧 `relations.js` 已删除，`index.html` 已更新引用

## v1.9.3 (2026-08-18)

### 关系图谱 — 交互逻辑修复与拖拽支持

- **清空后画布为空**：修复 `_getSelectedGraphSubjects` 返回空数组时，绘制逻辑回退到全选所有实体的问题。移除 `drawRelationsGraph`、`renderRelations`、节点点击关联计算中 3 处 `selectedSubjects.length > 0 ? ... : allEntities` 回退分支，清空后画布正确显示"请勾选主体或添加词条"空状态提示
- **首次加载默认全选**：`_getSelectedGraphSubjects` 区分 `undefined`（首次加载，自动全选）与 `[]`（手动清空，画布为空），首次进入关系图谱默认显示全部实体
- **节点拖拽支持**：左键按下节点进入待定状态，鼠标移动超过 3px 阈值后确认拖拽（光标变 grabbing），未超过阈值松开则视为点击执行选中/取消选中。拖拽与点击互不冲突，拖拽时节点跟随鼠标实时重绘

### 架空地图模拟器 — 拖拽修复与交互增强

- **拖拽坐标修复**：拖拽和滚轮缩放统一使用舞台坐标系（`_MAP_W / rect.width` 比例换算），修复 CSS 缩放后拖拽穿帮
- **`_clampViewport` 重写**：使用舞台坐标计算，`panX ∈ [_MAP_W - mw, 0]`、`panY ∈ [_MAP_H - mh, 0]`，zoom=1 时 panX=0 地图刚好填满
- **居中定位修复**：`_mapHighlightLoc` 居中使用 `_MAP_W/2` 舞台坐标，先 clamp 再 apply
- **标注地点按钮换色+可取消**：激活时显示橙色（`btn-warning`），再次点击取消回到选择模式
- **信息统计字体加大**：从 12px 改为 14px
- **查看详情在左下角信息框显示**：所有 `showPreviewCard` 调用改为 `_mapShowDetail`，在 stats 区域显示详情（地点/势力/人物/物品），点击"← 返回统计"回到统计界面
- **新增 CSS**：`btn-warning` 橙色按钮样式

### 架空地图模拟器 — 布局优化与统计面板

- **布局恢复左右结构**：地图左侧 + 右侧编辑栏，左侧地图框高度贴合地图内容（无多余背景），下方空白区显示统计信息
- **统计面板**：地图下方 `.fmap-stats` 区域显示选中维度的统计数据（地点类型分布、关联势力/人物、领地势力分布等），用 `|` 分隔各维度
- **信息显示设置**：工具栏新增「📊 信息显示」按钮，点击弹出下拉勾选列表，可切换 6 个统计维度（地点类型/关联势力/关联人物、领地势力/关联人物/关联地点），设置持久化到 `state.data._mapInfoOpts`
- **去除背景**：画布背景改为纯白 `0xffffff`，CSS 背景也改为白色，不再有蓝色/羊皮纸色边框穿帮
- **拖拽边界约束修复**：`_clampViewport()` 修复向左/向上拖拽穿帮，地图比画布小时居中，地图比画布大时严格限制 panX/panY 不超出 `[canvasSize - mapSize, 0]` 范围
- **居中后先 clamp**：`_mapHighlightLoc` 定位地点后先调用 `_clampViewport()` 再应用视口，防止居中后穿帮
- **领地高亮闪烁**：点击地点列表定位时，对应领地用白色半透明遮罩闪烁 1.5 秒（`_mapFlashTerritory`），替代原来的 toast 提示
- **删除世界系统关联**：地点管理和领地面板均移除"关联世界系统"区块（`worldSystemIds`），背包物品选择已完全覆盖此功能
- **数据迁移**：`_ensureMapData` 中 `delete t.worldSystemIds` 清理旧数据

## v1.9.0 (2026-08-17)

### 架空地图模拟器 — 交互与数据增强

- **资源库预览图**：保存到资源库时自动生成含地形、地点标记、领地名称的 PNG 预览图（`imageData`），资源库中可直接查看缩略图
- **领地关联多选词条**：领地详情面板从单选下拉改为多选词条型，支持关联势力（`factionIds`）、地点（`locationIds`）、人物（`characterIds`）、世界系统（`worldSystemIds`），每个关联显示为可点击词条标签，点击弹出 `showPreviewCard` 小卡片
- **领地背包物品**：领地详情面板新增背包物品选择区域（`t.backpackItems`），与人物列表的"背包+关联"模式一致，每个世界系统显示物品多选
- **地点管理背包物品**：地点编辑表单新增背包物品选择（`loc.backpackItems`），与人物列表的"背包+关联"模式一致，wiki 查看模式也显示背包物品
- **地点标记 emoji 自定义**：地点标记支持自定义图标（`marker.icon`），默认 📍，点击 🎨 按钮通过 `customPrompt` 修改，渲染时用 PIXI.Text 绘制 emoji 图标替代原来的红色圆点
- **地点标记缩小**：标记圆点从 7px 改为 14px emoji 字体，名称标签从 11px 缩小到 10px，偏移从 -16 调为 -12
- **点击地点标记弹小卡片**：点击地图上的地点标记弹出浮动小卡片（`_mapShowLocCard`），显示地点名称、分类、描述摘要，可跳转到地点详情或关闭
- **地点列表点击定位**：侧栏地点列表点击条目自动平移视口到对应位置（`_mapHighlightLoc`），同时选中该地点所在的领地
- **去除蓝色背景**：地图画布 CSS 背景从 `#d4e6f1` 改为 `#f0ece4` 羊皮纸色，PIXI 初始化背景色同步修改，未生成地形时显示居中提示文字
- **100% 最小缩放**：滚轮缩放下限从 10% 改为 100%，防止地图缩小到不可见
- **拖拽边界约束**：新增 `_clampViewport()` 函数，拖拽和缩放时限制视口不超出地图边界，防止露出背景穿帮
- **世界系统预览卡片**：`showPreviewCard` 新增 `worldSystem` 类型，点击世界系统词条弹出预览卡片；世界探索器新增 `renderExplorerWorldSystemDetail` 详情页，搜索/推荐也支持世界系统
- **领地数据结构迁移**：`factionId` → `factionIds[]`、`locationId` → `locationIds[]`，新增 `characterIds[]`、`worldSystemIds[]`、`backpackItems{}`，`_ensureMapData` 自动迁移旧数据
- **删除世界跳过入场动画**：`showScreen('home', true)` 新增 `skipAnimation` 参数，删除世界后直接显示首页不播放入场动画
- **创建世界同名检测**：`createProject()` 检查 `state.projects` 是否已存在同名世界，存在则 `showToast` 提示

## v1.8.0 (2026-08-17)

### 架空地图模拟器 — Simplex Noise 地形重构

- **边缘衰减**：heightmap 生成后对地图四周 12%（宽）/ 15%（高）范围施加衰减函数，高度值乘以衰减系数并减去 0.18 偏移，确保地图边缘全部变为海洋，呈现世界地图展开图效果
- **连通分量分析**：用 BFS 扫描所有陆地区域，标记为不同连通分量（`componentMap`），过滤面积 < 300px 的微小岛屿。领地种子点仅在所属连通分量内竞争最近距离，彻底杜绝领地跨越两个大陆的问题
- **噪声扭曲 Voronoi**：用第二组 Simplex Noise（`seed + 42`）对每个像素坐标施加 ±22px 的扭曲偏移后再计算最近种子点，原本笔直的 Voronoi 分界线变为自然曲线，符合现实领土分割
- **统一陆地色**：移除沙滩/平原/丘陵/山地/雪线的地形渲染层级，陆地统一使用羊皮纸色 `(218, 202, 172)`，仅保留海洋深度渐变
- **海岸线描边**：检测陆地与海洋的交界像素，绘制深蓝灰色 `(72, 88, 108)` 海岸线
- **领地质心标签**：生成领地时遍历 territoryMap 计算每个领地所有像素的质心（`centerX/centerY`），标签定位到质心而非种子点，避免标签偏到领地边缘
- **删除绘制功能**：移除 `✏️ 绘制` 按钮、绘制提示条、`_mapDrawClick`、`_mapFinishDraw`、`_mapCancelDraw`、`_mapDrawGfx`、`_mapDrawPoints` 等全部绘制相关代码和变量
- **地图尺寸调整**：`_MAP_W` 从 800 调整为 1000，更接近世界地图横宽比例
- **数据结构更新**：`worldMap` 默认值移除 `bgType`，新增 `genCount` 字段

## v1.7.1 (2026-08-14 晚)

### 关系图表勾选优化

- **可搜索下拉弹窗**：移除内联 checkbox 标签区域，改为 `filter-bar` 按钮弹窗形式（👤 角色、🏰 势力、📍 地点、📦 物品、⚡ 事件），每个类型一个按钮，点击弹出可搜索下拉勾选列表
- **选中计数**：按钮上显示各类型选中数量（如 `👤 角色(3)`），选中时按钮高亮
- **默认全选**：`_getSelectedGraphSubjects()` 首次调用自动初始化为所有实体全选，筛选按钮上即时显示计数
- **移除标签区域**：统一使用按钮弹窗形式，不再显示已选主体标签列表
- **局部刷新**：`toggleGraphSubjectFromFilter()` 勾选后局部更新按钮状态和图表，不关闭弹窗

### Emoji 库面板式管理

- **管理面板**：从简单内联展示改为面板式弹窗管理（类似图标选择器），包含自定义输入框 + 分类 Emoji 面板（武器、魔法、药剂、食物、装备、工具、书籍、自然、动物、杂项）
- **每个类别末尾加号按钮**：点击聚焦到自定义输入框
- **拖拽排序**：⭐ 我的 Emoji 区域支持拖拽调整顺序，拖拽时高亮目标位置
- **右键删除**：右键点击已添加的 Emoji 弹出上下文菜单，包含 🗑️ 删除选项
- **保存/取消**：保存才持久化（`autoSave()`），取消恢复最初备份（`_emojiLibBackup`，仅在首次打开时创建，防止重新渲染覆盖备份）
- **布局修复**：`emoji-grid` 添加 `padding: 4px`，防止删除按钮被裁剪

### Bug 修复

- 修复 Emoji 库取消操作未真正取消（每次 `openEmojiLibManager()` 重新渲染覆盖了原始备份）
- 修复 Emoji 库关闭程序后自定义 Emoji 丢失（取消恢复备份后未调用 `renderTabContent()` 刷新界面）

---

## v1.7.0 (2026-08-14 下午)

### 关系图表优化

- **重置布局按钮**：新增 `resetGraphLayout()` 函数，一键重置所有节点位置为圆形布局
- **图表缩放**：从 CSS `transform:scale` 改为 Canvas 视口缩放（`ctx.translate` + `ctx.scale`），新增 `_getGraphViewport()` 管理视口状态（zoom、panX、panY）
- **滚轮缩放**：以鼠标位置为中心点缩放，缩放范围 15%~400%
- **画布平移**：左键点击空白处或右键/中键拖拽画布平移
- **节点拖拽**：左键点击节点可拖拽到任意位置，缩放后坐标正确转换（`_screenToWorld()`）
- **头像显示**：节点内显示角色头像（`_loadAvatarForGraph()`），圆形裁剪 + 边框，无头像时显示图标
- **图例固定左上角**：图例绘制移到 `ctx.restore()` 之后，不受视口变换影响，添加半透明背景

### 资源库图片查看器优化

- **拖拽移动**：放大后可拖拽图片移动查看不同区域，支持鼠标拖拽平移（`_imageViewerPanX/PanY`）
- **滚轮缩放**：在图片查看器内支持滚轮缩放
- **按钮缩放/重置**：缩放和重置按钮自动归零平移
- **居中显示**：`transform-origin` 改为 `center center`，配合 `translate()` 实现居中+平移

### 资源库功能增强

- **初始无选中状态**：资源列表初始 `selectedId = null`，所有项均为未选择
- **资源 CRUD**：新建资源（`addResource()`，弹窗表单含标题/类别/备注/图片/自定义属性）、编辑资源（`editResource()`，支持更换图片/移除图片）、删除资源（`deleteResource()`，确认后删除并清除选中状态）
- **资源关联词条**：资源详情显示关联词条列表（`_renderResourceLinkedEntries()`），关系图类资源自动从 `graphSubjects` 提取关联实体，点击词条可弹出预览卡片
- **关系图类别常驻**：`SYSTEM_LOCKED_CATEGORIES` 定义系统常驻类别（关系图、头像），属性定义中锁定不可编辑/删除（`systemLocked: true`，🔒 图标标识）
- **头像自动保存**：角色头像上传时自动保存到资源库，命名格式"角色名头像"（`autoSaveAvatarToResources()`），已有同名资源则更新图片
- **自定义 Emoji 库**：新增 Emoji 库管理（`_getEmojiLib()`、`renderEmojiLibSection()`、`addEmojiToLib()`、`removeEmojiFromLib()`），支持自主添加/删除 Emoji，每个 Emoji 可设名称
- **Emoji 选择器集成**：资源标题输入框（`getEmojiPickerHtml()`）、物品图标选择器、百科图标选择器均集成自定义 Emoji 库，点击直接填入
- **资源保存到本地**：`saveResourceToLocal()` 支持图片资源下载为 PNG/JPG，非图片资源导出为 JSON
- **类别选择器**：新建/编辑资源时使用 `renderCategorySelect()` 下拉选择类别
- **自定义属性**：资源支持自定义属性字段（`renderCustomPropField` / `renderCustomPropWikiHtml`）
- **搜索筛选**：资源列表支持搜索框按标题/类别/备注筛选

### 世界探索入场动画

- **卡片入场**：`.explorer-card` 加入动画系统，Tab 切换时卡片从下方滑入 + 淡入，交错延迟 0.04s
- **头部入场**：搜索区域添加 `card` 类，与其他 Tab 一致有入场效果
- **详情页入场**：`showExplorerDetail()` 加载详情后触发完整入场动画（wiki-page 滑入、wiki-title 逐字动画、wiki-header 滑入、wiki-section 交错滑入）
- **搜索/换一批动画**：搜索结果、换一批推荐、返回列表时均有交错入场动画
- **动画系统扩展**：`animation.js` 新增 `.explorer-card` 和 `.wiki-page` 选择器支持

### Bug 修复

- 修复关系图表 Tab 切换失败（背包物品 `backpackItems` 为对象结构，代码错误按数组处理）
- 修复拖拽功能在缩放后坐标偏移（未考虑缩放比例对鼠标坐标的影响）

---

## v1.6.0 (2026-08-14)

### "现实幻想"→"世界百科"模块重构

- **模块重命名**：导航 Tab "现实幻想"→"世界百科"（📚），对应模块 encyclopedia.js 全面重构
- **三栏百科布局**：左侧大类列表 → 中间子类列表 → 右侧词条详情/编辑，三栏各自独立滚动
- **默认百科树**：内置 13 个大类（时代背景、重大事件、地理疆域、气候环境、政治制度、军事、经济、社会结构、科技生产力、文化思想、宗教信仰、民族族群、语言称谓），每个大类下预设子类
- **大类/子类管理**：支持新建、编辑、删除大类和子类，自定义图标选择器
- **词条 CRUD**：每个子类下可创建词条，支持标题、图标、描述、自定义属性编辑
- **模板快速添加**：`openAddTemplateCategoryModal()` 提供预设模板一键添加大类和子类，已有子类自动跳过
- **百科子类绑定**：`bindEncyclopediaCategory()` 将百科子类绑定到实体类型（角色、势力、地点等），绑定后该类型实体自动归入对应子类下管理
- **导航跳转**：`navigateToEncyclopediaItem()` / `navigateToEncyclopediaSub()` 支持从其他模块跳转到百科词条或子类
- **列表固定撑满页面**：`.encyclopedia-layout` 使用 `height: calc(100vh - 120px)`，三面板各自 `overflow-y: auto` 实现内部滚动

### 全局 alert → showToast 替换

- **问题**：`alert()` 阻塞导致焦点丢失，添加模板/同名检测后无法继续输入
- **修复**：全量替换 renderer 目录下所有 `alert()` 为非阻塞 `showToast()`
- **涉及文件**：encyclopedia.js、items.js、characters.js、factions.js、locations.js、races.js、narrative-events.js、relations.js、glossary.js、navigation.js、properties.js、constitution.js

### 世界系统模块重命名 & 图标更换

- **问题**："新建背包"命名出戏，🎒图标不符合系统概念
- **修复**：
  - 按钮文本"新建背包"→"新建系统"
  - 默认图标 🎒→🎲（骰子）
  - 全局"背包"→"系统"文字替换（items.js、properties.js、narrative-events.js、characters.js、overview.js）
  - emoji 选择器列表中的 🎒 保留（作为可选项）

### 编辑状态下物品添加按钮禁用

- **问题**：编辑物品时"+ 物品"按钮仍可操作，导致误操作
- **修复**：encyclopedia.js、items.js 中添加编辑状态判断，编辑时按钮 disabled + 半透明样式

### 世界规则模块（constitution.js）优化

- 列表固定撑满页面，内部滚动（`max-height: calc(100vh - 240px); overflow-y: auto`）
- 支持自定义字段（引用 `renderCustomPropField`）
- 新建条目后自动聚焦标题输入框

### 自定义字段支持百科子类绑定

- properties.js 引用型字段新增 `encyclopediaSub`（📚 百科子类）选项
- glossary.js `collectGlossary` 新增 `encyclopediaSub` 类型
- constitution.js 新增 `constitution`（📜 世界规则）作用域

### 大纲章节拖拽排序 & 插入

- **问题**：章节无法在卷内拖拽排序，也无法在指定位置插入
- **修复**：
  - 编辑模式下章节标题前新增 `⠿` 拖拽手柄（`ol-ch-drag`）
  - 每个章节新增 `⬆` 插入按钮
  - `setupOutline()` 扩展拖拽逻辑，区分卷拖拽（`volume`）和章节拖拽（`chapter`）
  - 新增 `insertOutlineChapter(vi, ci)` 函数

### 仪表盘数据统计优化

- **百科子类替代大类**：统计区从显示百科大类改为显示小类（子类），每个子类显示其下词条数
- **空项隐藏**：百科子类词条数为 0 时不显示，世界系统背包物品数为 0 时不显示
- **移除时间线统计**：删除"时间线"和"事件"两个重复统计项

### "世界宪法"→"世界探索"模块重构

- **模块重命名**：导航 Tab "世界宪法"（📜）→"世界探索"（🔭），constitution.js 全面重写为 Wiki 浏览器
- **全局搜索**：顶部搜索框可检索所有角色、地点、势力、种族、物品、事件、百科词条
- **随机推荐卡片**：从所有词条中随机抽取 12 个以卡片形式展示，点击直接查看详情
- **换一批按钮**：`refreshExplorerRecommendations()` 随机刷新推荐
- **专属详情页**：点击卡片在页面内直接展示 wiki 详情，不跳转对应 tab，纯只读浏览
- **浏览历史**：`_explorerHistory` 支持返回上一级
- **7 种实体详情**：角色、地点、势力、种族、物品、事件、百科词条各有专属渲染函数

### 地点管理布局优化

- **删除标签树**：移除左侧标签树面板（`renderTagTree`、`addTag`、`deleteTag`、`renderTagCheckboxes` 等函数）
- **左列表右详情**：改为与其他 tab 一致的 `char-layout` 布局（左列表+右详情）
- **样式清理**：移除 `.location-layout`、`.tag-tree-panel`、`.tag-tree-*` 等旧 CSS
- **AI 生成修复**：`aiGenLocation` 不再依赖 `#ai-location-result` 元素

---

## v1.5.0 (2026-08-13)

### 搜索框系统

- **全局搜索框**：为所有带词条的 Tab（人物、势力、地点、物品、种族、关系、事件、力量体系、大纲、时间线）添加搜索框
- **中文输入法兼容**：使用 `compositionstart` / `compositionend` 事件标记输入法组字状态，组字过程中不触发列表刷新，解决中文输入被打断问题
- **局部刷新优化**：`registerSearchTarget` + `_searchRefreshList` 机制实现搜索时仅刷新列表容器，避免 `renderTabContent()` 重建整个 DOM 导致输入框重复渲染和焦点丢失
- **搜索框位置**：搜索框统一放置在筛选按钮上方，视觉层次清晰

### 关联背包物品筛选

- **二级弹窗筛选**：弹窗列表显示背包，点击背包展开二级搜索框加物品列表，支持多选
- **弹窗定位修复**：弹窗采用 `position: fixed` 定位，参照按钮 `getBoundingClientRect()` 计算位置，克隆弹窗到 `document.body` 避免父容器 `overflow` 影响
- **弹窗交互修复**：修复弹窗默认开启、点击按钮无法关闭、列表随鼠标位置变动等问题

### 属性定义优化

- **弹窗表单化编辑**：自定义词条的编辑和新建从分步操作改为一页弹窗表单完整创建与编辑
- **词条类型卡片式选择**：radio 按钮隐藏，改为三列等宽卡片式选择，上方 emoji 图标、中间加粗类型名、下方灰色描述
- **"未知"选项锁定**：选项名为"未知"时，名称和简述输入框均设为 `readonly` + 灰色背景，防止误改
- **应用模块勾选样式**：选中状态改为浅灰底 + 黑色粗边框，未选中为白底 + 细灰边框，添加过渡动画
- **输入框拖拽防护**：所有输入框添加 `ondragstart="event.stopPropagation();return false"` + CSS `-webkit-user-drag: none`，防止文字被拖入其他输入框覆盖内容
- **拖拽排序**：类别、稀有度、规模、自定义词条选项均支持拖拽排序，`⠿` 拖拽手柄操作，拖拽时源行半透明、目标行高亮边框
- **插入功能**：所有列表行添加 `＋` 插入按钮，在当前位置前插入空白项并自动聚焦名称输入框
- **删除同步回"未知"**：删除选项/稀有度/规模时，已选该选项的实体自动改回"未知"，支持单选和多选
- **左侧文字同步**：修改类别名称后直接更新 `.prop-index` 元素文字，无需切换 Tab
- **排序后序号更新**：拖拽排序完成后调用 `renderTabContent()` 重新渲染，序号自动更新

### Bug 修复

- 修复搜索框输入一个字出现两个的问题（DOM 重建导致重复渲染）
- 修复弹窗错位问题（参照按钮位置固定不变）
- 修复筛选按钮与标题排版不在同一行的问题
- 修复删除选项后已选该选项的实体值未同步的问题

---

## v1.4.0 (2026-08-12)

### 导航栏重构

- **移除冗余 Tab**：删除"故事设计"和"伏笔回收"标签页及对应 AI 生成功能（aiGenerateStory、aiGenerateForeshadowing）
- **Tab 高度均分**：使用 `flex: 1` 让剩余 Tab 均匀填满导航栏垂直空间，消除底部空白
- **宽度调整**：导航栏宽度从 200px 逐步优化至 140px，更紧凑
- **图标与文字对齐**：图标和文字统一 14px 字号，流内布局（gap: 6px）保证图标文字不分离
- **整体居中**：`justify-content: center` + `align-items: center` 实现上下左右居中
- **整体左移**：`margin-left: -6px` 微调视觉居中感
- **文字动画修复**：`animateActiveNavText` 添加 `complete` 回调，动画结束后清除残留 `transform`，解决逐字动画导致文字倾斜的问题

### 章节关联优化

- **标签点亮形式**：章节关联从复选框列表改为标签（tag）高亮形式，节省空间
- **仅显示卷内关联**：`renderVolTags()` 函数只显示当前卷包含的关联项，非卷内项以虚线低透明度样式展示
- **点击切换关联**：点击标签即可切换该章节与条目的关联状态

---

## v1.3.0 (2026-08-11)

### 事件系统重构

- **前置/后续事件独立显示**：前置事件和后续事件从关联面板中移出，作为 Wiki 视图和编辑表单的独立区块显示，仅展示关系简述
- **定位到时间线**：新增 `locateEventOnTimeline()` 功能，Wiki 视图底部和嵌套卡片中均可一键跳转到时间线并高亮定位事件
- **关联面板精简**：关联面板仅保留人物、势力、地点、物品四类

### 物品选择流程优化

- **背包-物品二级选择**：物品选择模态框改为先显示背包列表，点击背包展开对应物品，支持折叠/展开动画

### 时间线拖拽排序

- **自定义拖拽系统**：从 HTML5 Drag API 改为 mousedown/mousemove/mouseup 实现，拖拽更流畅
- **点击/拖拽分离**：点击展开详情，按住拖拽排序，互不干扰
- **拖拽视觉反馈**：ghost 元素跟随鼠标，紫色插入指示线显示放置位置
- **监听器管理**：全局 mousemove/mouseup 监听器使用 window 引用，避免重复绑定

### 时间线排序算法

- **Kahn 拓扑排序**：重写 `_tlAutoSort()`，使用 Kahn 算法替代错误的 DFS
- **约束规则**：前置事件必须在上方，后续事件必须在下方，无关事件可自由插入中间位置
- **用户优先级**：以用户拖拽顺序为优先级，仅在违反约束时自动调整
- **上早下晚**：时间线排序规则明确为"上早下晚"

### Bug 修复

- 修复关联面板仅在选中子事件后才渲染的问题
- 修复拖入时间线时事件丢失的问题（`state.data.timelineOrder` 未及时更新）
- 修复点击展开/折叠与拖拽冲突导致卡片无法展开的问题
- 修复删除事件后再拖入导致所有事件被加入时间线的问题

---

## v1.2.0 (2026-08-10)

### 事件管理

- 事件 Wiki 视图与编辑表单
- 事件关联（人物、势力、地点、物品）
- 前置事件、后续事件、子事件关系
- 事件类型自定义属性

### 时间线

- 时间线视图基础功能
- 事件拖拽加入/移出时间线
- 事件卡片展开/折叠

---

## v1.1.0 (2026-08-08)

### 世界系统

- 人物、势力、地点、物品管理
- 背包系统
- 种族管理
- 世界宪法

---

## v1.0.0 (2026-08-06)

### 初始版本

- Electron 桌面应用框架搭建
- 世界观构建基础功能
- AI 辅助生成
- 数据持久化与自动保存